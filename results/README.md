# Machine-readable results

This directory contains only compact outputs needed to cross-check values reported in the manuscript and Supplementary Information. Formatted manuscript tables are not duplicated here.

The suffix `frozen_dl` identifies removal outputs recalculated directly from the frozen public cohort using the reported variance-imputation procedure. The Axis 2 file marked `v2` is the corrected abundance result used by the final manuscript.
